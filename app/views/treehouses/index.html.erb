<% @treehouses.each do |treehouse| %>
  <li>
    <%= treehouse.name %>
  </li>
<% end %>

<% @treehouses.each do |treehouse| %>
  <li>
    <%= treehouse.name %>
    <div class="treehouse-photos">
      <% treehouse.photos.each do |photo| %>
        <%= image_tag photo, class: "treehouse-photo" %>
      <% end %>
    </div>
  </li>
<% end %>

<%= link_to "Add treehouse", new_treehouse_path %>
