
<% @bookings.each do |booking| %>
  <div>
    <li>
      <h3>Booking</h3>
      <p> From: <%= booking.start_date %> To: From: <%= booking.end_date %></p>
    </li>
    <%= link_to 'Approve', approve_treehouse_booking_path(@booking, @treehouse), data: {turbo_method: :post}%>
  </div>
<% end %>


<%= link_to "Add treehouse", new_treehouse_path %>
